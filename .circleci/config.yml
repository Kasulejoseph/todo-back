version: 2.1
orbs:
  node: circleci/node@3.0.0
workflows:
  node-tests:
    jobs:
      - build

jobs:
  build:
    working_directory: ~/todo-back
    docker:
      - image: circleci/node:10.16.3
      - image: circleci/postgres:9.6-alpine-ram
        environment:
          POSTGRES_USER: kasule
          POSTGRES_DB: tododb_test
          POSTGRES_PASSWORD: password
    steps:
      - checkout
      - run:
          name: start-database-serve
          command: 'pg_ctl -D /dev/shm/pgdata/data -l logfile start'
      - run:
          name: update-npm
          command: 'sudo npm install -g npm@5'
      - restore_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
      - run:
          name: install-npm-wee
          command: npm install
      - save_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
          paths:
            - ./node_modules
      - run:
          name: test
          command: npm test
      - run:
          name: code-coverage
          command: './node_modules/.bin/nyc report --reporter=text-lcov'
      - store_artifacts:
          path: test-results.xml
          prefix: tests
      - store_artifacts:
          path: coverage
          prefix: coverage
      - store_test_results:
          path: test-results.xml
